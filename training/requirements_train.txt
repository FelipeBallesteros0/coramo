# Dependencias para entrenar el modelo openWakeWord "coramo"
# Instalar con: pip install -r requirements_train.txt

# openWakeWord con soporte de entrenamiento completo
openwakeword[full]

# TFLite runtime para exportar el modelo final
ai-edge-litert

# Piper TTS para generacion de samples sinteticos
piper-tts

# Descarga de datasets desde HuggingFace
datasets<4
huggingface_hub
soundfile

# Audio y procesamiento
librosa
soundfile
scipy
numpy
